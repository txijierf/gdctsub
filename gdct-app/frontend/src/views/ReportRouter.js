import React from 'react';

import { Switch, Route } from 'react-router-dom';

const ReportRouter = () => (
  <Switch>
    <Route exact path="/report" component={null} />
  </Switch>
);

export default ReportRouter;
